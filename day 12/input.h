#include <stdio.h>

typedef struct
{
    char word[100];
}
string;


void get_string(void)
{
    // Getting input from the user.

    // printf("Enter the lenght of the string: ");
    // // Taking input in the form of char
    // string word;
    // int length = scanf("%d", &length);
    
    // // Taking input of an array of characters through a for loop.

    // for (int i = 0; i < length; i++)
    // {   
    //     word.word[i] = scanf("%c", &word.word[i]);
    // }   
    // return word.word[];
}
