#include <stdio.h>

int main(void)
{
    // if only one number
        // quit
    // else
        // sort left half of number
        // sort right half of numbers
        // merge sorted halves
    return 0;
}