#include <stdio.h>

int check(int n);

void main(void)
{
    int n;
    do
    {
        int n = check(5);
    }
    while(n < 1);
    printf("Hello world and no one is best in the name of god god god god god \
so let's get started \n");
}

int check(int n)
{
    printf("%i\n",n);
    return 0;
}
